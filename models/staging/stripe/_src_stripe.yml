sources:
  - name: stripe
    database: raw
    schema: stripe
    description: "The source data for the stripe project."
    tables:
      - name: payment
        description: "A table containing payment information."
        config:
          loaded_at_field: _batched_at
          freshness:
            warn_after: {count: 12, period: hour}
            error_after: {count: 24, period: hour}        